# Variáveis de ambiente para a API TCC (opcional)
# Renomeie para .env e configure conforme necessário
# Em produção, use getenv() no settings.php

DB_NAME=tcc_db
DB_HOST=127.0.0.1
DB_USER=root
DB_PASS=
DB_PORT=3306
