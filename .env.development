# Dev: npm run dev — frontend local + API via proxy
VITE_API_BASE_URL=/api

# Usa o banco de produção (Hostinger) no PC, sem MySQL remoto local
VITE_API_PROXY_TARGET=https://nexora.conectaxcon.com.br
